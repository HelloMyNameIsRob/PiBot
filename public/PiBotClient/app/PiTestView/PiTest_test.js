'use strict';

describe('PiBot.PiTestView module', function() {

  beforeEach(module('PiBot.PiTestView'));

  describe('PiTest controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var piTestCtrl = $controller('PiTestCtrl');
      expect(piTestCtrl).toBeDefined();
    }));

  });
});