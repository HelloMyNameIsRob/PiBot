'use strict';

describe('PiBot.PiBotView module', function() {

  beforeEach(module('PiBot.PiBotView'));

  describe('PiBot controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var piBotCtrl = $controller('PiBotCtrl');
      expect(piBotCtrl).toBeDefined();
    }));

  });
});